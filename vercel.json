{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "buildCommand": "apt-get update && apt-get install -y cmake"
      }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
